<section id="comment-area">
    <h2>Comments</h2>
    <%= render partial: '/comments/comment', collection: commentable.comments %>

    <%= form_for [commentable, Comment.new], layout: :basic do |form| %>
        <%= form.hidden_field :commentable_id, value: commentable.id %>
        <%= form.hidden_field :commentable_type, value: commentable.model.class %>

        <div class="media comment">
            <div class="media-left">
                <%= gravatar_image_tag current_user.email,
                    alt: current_user.name,
                    gravatar: { default: :identicon, size: 50 }
                %>
            </div>
            <div class="media-body">
                <%= form.text_area :body, class: 'comment-body', label: 'New Comment', placeholder: 'Comment' %>

                <div class="row">
                    <div class="col-sm-3 col-sm-push-9">
                        <%= form.submit class: 'btn btn-primary btn-block' %>
                    </div>
                    <div class="col-sm-9 col-sm-pull-3">
                        <p class="small">Comments are formatted using
                        <a href="http://daringfireball.net/projects/markdown/syntax">Markdown</a>.
                        All <span class="initialism">HTML</span> tags will be sanitized.</p>
                    </div>
                </div>
            </div>
        </div>
    <% end %>
</section>

